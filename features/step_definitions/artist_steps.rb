Given /^there are (\d+) Artist$/ do |total|
  pending
end

When /^a user makes a (GET|POST|PUT|DELETE) request to "([^"]*)"$/ do |action, page_name|
  pending
end

And /^the JSON from the (server|client) is valid$/ do |source|
  pending
end

Then /^Artist is (\w+)$/ do |action|
  pending
end

And /^the response code is (\d+)$/ do |response_code|
  pending
end
